// Цей скрипт призначений для вузла "Code" в n8n.
// Він отримує дані з Webhook (запит від вашого мобільного додатку),
// обробляє їх та формує правильну відповідь, яку очікує додаток.

// 1. Отримуємо повідомлення, яке надіслав користувач з додатку
const userMessage = $input.first().json.body.message || "Порожнє повідомлення";

// 2. Тут ви можете додати власну логіку.
// Наприклад, якщо ви НЕ використовуєте вбудовані вузли OpenAI/AI Agent в n8n,
// ви можете згенерувати або обробити відповідь прямо тут у коді.
// У цьому прикладі ми просто беремо текст користувача і формуємо тестову відповідь.
const botResponseText = `Я отримав ваше повідомлення: "${userMessage}". Це відповідь безпосередньо з вашого n8n сервера!`;

// 3. Формуємо фінальний об'єкт (JSON), який потрібно повернути.
// Мобільний додаток (chat.tsx) запрограмований очікувати поле "reply".
return {
  json: {
    reply: botResponseText
  }
};
